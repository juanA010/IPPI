<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="parentapp.ippi.ippiparent">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AppMenu" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".BookingHistoryActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".SignUpActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Homepage"
            android:label="@string/title_activity_homepage" />
        <activity android:name=".SearchbabysitterActivity" />
        <activity android:name=".ChangeEmailActivity" />
        <activity android:name=".ChangePassActivity" />
        <activity android:name=".ChangeLocationActivity" />
        <activity android:name=".ResetPasswordActivity" />
        <activity android:name=".DeleteAccountActivity" />
        <activity android:name=".MainPageActivity" />
        <activity
            android:name=".ParentsMapActivity"
            android:label="Find your Babysitter" />
        <activity android:name=".AvailableSitterActivity" />
        <activity android:name=".SitterProfileActivity" />
        <activity android:name=".SitterContactActivity" />
        <activity android:name=".NoAvailableSitter" />
        <activity
            android:name=".NavigationToSitter"
            android:label="@string/title_activity_navigation_to_sitter" />
        <activity android:name=".Retrieve" />
        <activity android:name=".SitterOnServiceActivity" />
        <activity android:name=".RequestTimeActivity" />
        <activity android:name=".TrackingMapActivity" />
        <activity android:name=".GiveRatingActivity" />
        <activity android:name=".MessagesActivity" />
        <activity android:name=".MessagesActivityDetails" />
        <activity android:name=".BookingHistoryDetail" />
        <activity android:name=".ReciptActivity"></activity>
    </application>

</manifest>